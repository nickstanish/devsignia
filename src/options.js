import ReactDOM from 'react-dom';
import React from 'react';
import OptionsApp from './components/OptionsApp';

const element = document.getElementById('app');
ReactDOM.render(<OptionsApp />, element);